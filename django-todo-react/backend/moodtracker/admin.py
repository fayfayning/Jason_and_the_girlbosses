from django.contrib import admin
# from .models import # Whatever model classes we make

# Register your models here.

""" 
Syntax for this should show all of the model classes that we make
kinda like this for each:

class whateverAdmin(admin.ModelAdmin):
    list_display(names of our member variables in the model w strings)

admin.site.register(whatever, whateverAdmin)
"""